{
  "name": "amdine",
  "version": "1.1.1",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "push": "npm run build && npm publish && npm run clean"
  },
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "globby": "^13.1.1"
  },
  "devDependencies": {
    "typescript": "^4.5.5",
    "@types/node": "^17.0.21"
  }
}
