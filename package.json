{
  "name": "amdine",
  "version": "1.5.0",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "push": "npm run build && npm publish && npm run clean",
    "test": "tsm test.ts"
  },
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "globby": "^13.1.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.21",
    "tsm": "^2.2.1",
    "typescript": "^4.5.5"
  }
}
